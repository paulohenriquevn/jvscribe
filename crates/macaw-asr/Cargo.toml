[package]
name = "macaw-asr"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Adapter de inferência ONNX — descartável por construção (M2 pode substituir)"

[dependencies]
ort = { workspace = true }
ndarray = { workspace = true }

[dev-dependencies]
macaw-audio = { path = "../macaw-audio" }
hound = { workspace = true }
