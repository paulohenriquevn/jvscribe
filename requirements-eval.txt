# Deps de avaliação de M1 (auditadas: wiki/medicoes/m1-harness.md)
# WER + normalização (Fase 3)
jiwer>=3.0,<4.0
soundfile>=0.12,<1.0
numpy>=1.24,<3.0
# Baseline real (Fase 4): faster-whisper (ASR, sem torch) + leitura direta de
# parquet do HF (evita datasets>=5.0, que exige torchcodec/torch para decodar áudio)
faster-whisper>=1.0,<2.0
huggingface_hub>=0.20
pyarrow>=14.0
# Relatório de medição por template (`corpus/templates/*.md.j2`): a prosa dos caveats tem
# CONDIÇÕES, e f-string as esconde. Já vinha transitivo (torch, mkdocs, spacy); declarado
# porque agora é uso direto — depender de dependência-de-dependência quebra em ambiente limpo.
jinja2>=3.1,<4.0
