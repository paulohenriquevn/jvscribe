# Authoritative domains for WebFetch during /discover-execute.
#
# Format: one domain per line, no protocol, no trailing slash. Lines starting
# with '#' are comments. Wildcards via *.example.com are supported by the skill
# that reads this list.
#
# Default policy: empty allowlist = no WebFetch during discovery. Each project
# adds the domains its discovery surface needs. The list is intentionally narrow
# so discoveries cite authoritative sources, not random blogs.
#
# Suggested seed entries (uncomment + add the ones relevant to your domain):
#
# github.com
# raw.githubusercontent.com
# docs.python.org
# pkg.go.dev
# developer.mozilla.org
# www.rfc-editor.org
# kubernetes.io
# cloud.google.com
# aws.amazon.com
# learn.microsoft.com

# --- Habilitado 2026-07-30 para o ciclo DISCOVER de M9 (governança de artefato,
# --- CI de monorepo Rust+Python, model cards). Domínios autoritativos apenas.
github.com
raw.githubusercontent.com
docs.github.com
doc.rust-lang.org
docs.python.org
k2-fsa.github.io
huggingface.co
arxiv.org
mlflow.org
