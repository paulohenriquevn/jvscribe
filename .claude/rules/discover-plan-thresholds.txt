# Discover-Plan Score Thresholds — per-project tuning.
#
# Authoritative source: rules/discover-plan-golden-rule.md
#
# Verdict-band format (one entry per line), read by
# skills/discover-plan-confidence/scripts/run_discover_plan_score.py
# (`_parse_thresholds` splits on `|`):
#
#   VERDICT_TOKEN | MIN_SCORE
#
# The band whose MIN_SCORE the final score meets (highest first) wins;
# below the lowest band the verdict falls through to INVALID.
# Lines starting with `#` and blank lines are skipped. Lines without a
# `|` (the soft/hard-cap notes below) are ignored by the band parser —
# they are documentation; the hard caps themselves are computed in code
# (run_discover_plan_score.py) and in the checker scripts, per the golden rule.
#
# Per-project overrides go here and are CHANGELOG-tracked.

# --- Soft/hard-cap reference (documentation only; enforced in code) ---
# soft_cap.question_count_low = 3
# soft_cap.coverage_corner_thin = 1   # questions/corner below which a soft cap fires
# hard_cap.question_count_max = 15
# hard_cap.fabricated_citation = 49
# hard_cap.empty_corner = 49

# --- Verdict bands (parsed) ---
SHIPPABLE | 90
SHIPPABLE_WITH_CAVEATS | 70
NEEDS_REVISION | 50
INVALID | 0
