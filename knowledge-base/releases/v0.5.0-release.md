# Release v0.5.0

**Date:** 2026-07-27
**Verdict:** RELEASED (local — repositório sem remote; release = merge local `develop → main` + tag anotada)
**Source review:** `knowledge-base/reviews/m4-finalist-and-phoneme-head-review-2026-07-27.md` (READY_TO_MERGE)
**Merge commit (main):** 185db00
**Tag:** v0.5.0 (anotada, aponta para o merge commit)
**Mecanismo:** `git commit-tree develop^{tree} -p main -p develop` + `update-ref refs/heads/main` (sem checkout; HEAD permanece em `develop`; hook de `main` não dispara) — mesmo padrão dos releases v0.1.0–v0.4.0.
**GitHub release:** N/A (sem remote).

## Conteúdo (marco: M4 fechado)

- **ADR 0002 — finalista de arquitetura decidido por medição:** Zipformer-CTC small (22M), int8.
  Head-to-head vs Conformer-CTC medium: WER 28,86% vs 31,57%, IC95% bootstrap [2,11, 3,31] (exclui 0).
- **Cabeça de fonema auxiliar (M4 fase 3):** WER 29,97% → 28,58% (−4,63% rel), DoD ≥3% atingida no
  ponto (PASS com nota, IC fronteiriço). Modelo exportado + verificado em `models/m4-final-phoneme-small/`.
- **Conformer-CTC (2º finalista)** treinado + decodado; **curva Zipformer** completa (retornos decrescentes).
- Trabalho de **M6** (runtime v0 em Rust, fix ORT_DYLIB, blueprint de streaming causal) e **M3** (corpus).

## Correção de rota registrada

A derivação automática de versão (`compute_next_version` via `git describe`) retornou v0.0.0
(fallback) porque as tags v0.1.0–v0.4.0 vivem em `main` (descendentes dos pontos de release da
develop) e não são alcançáveis a partir de `develop`. O valor correto — **v0.5.0** (minor sobre
v0.4.0) — foi determinado por `git tag -l` + seções do CHANGELOG. A promoção inicial errada para
[0.1.0] foi desfeita (`git reset --soft`) antes de qualquer merge para `main`.

## Roadmap

- `ROADMAP.md` M4 `[ ]` → `[x]` (commit na develop pós-release).
