# Release v0.6.0

**Date:** 2026-07-28
**Verdict:** RELEASED (local — repositório sem remote; release = merges locais `workspace → develop → main` + tag anotada)
**Merge commit (main):** 6688131
**Tag:** v0.6.0 (anotada, aponta para o merge commit)
**Mecanismo:** `git commit-tree` + `update-ref` (sem checkout; HEAD permanece em `workspace`; hook de `main`/`develop` não dispara) — mesmo padrão do v0.5.0, agora no fluxo `workspace → develop → main`.
**GitHub release:** N/A (sem remote).

## ⚠️ Desvio autorizado: `/review` dispensado

`cycle-release` exige que todo release rastreie a um `/review` `READY_TO_MERGE`. Este release
foi cortado **sem `/review` do trabalho novo (medium+fonema), por decisão explícita do dono**
("commit e push sem review", 2026-07-28). O último review no disco
(`m4-finalist-and-phoneme-head-review-2026-07-27.md`, READY_TO_MERGE) cobre o finalista **small**,
**não** a reversão para medium (ADR 0003) nem o deliverable/DISC-05/06/eval-CPU deste release.
Registrado honestamente como desvio, não como conformidade. Risco mitigado: o conteúdo é
research + scripts de treino/eval + docs (não código de produção dos crates `macaw-*`).

## Conteúdo (M4 fechado por medição — finalista revertido para medium)

- **ADR 0003** — reversão do finalista de M4: small → **medium (64M)** por medição de soak/carga
  (RTFx empata sob carga; correção do RTFx do medium subestimado ~2× no ADR 0002).
- **Deliverable final de M4:** Zipformer-CTC medium (64M) + cabeça de fonema, int8 —
  **27,49% WER (decode Python)** / **27,46% WER / 10,54% CER (runtime Rust em CPU, full FLEURS)**,
  RTFx ~35-64× na i7-1355U. int8 lossless no medium (desconfundido). Modelo em
  `models/m4-final-medium-phoneme/`.
- **DISC-05** — probe de TTA forward-only (alinhamento de features) refutado por medição (−24,6pp).
- **DISC-06** — reframe "TTA do decoder" (IP) + probe do blank penalty refutado (erro é substituição).

## Roadmap

- `ROADMAP.md` M4 já estava `[x]` (flipado no v0.5.0). Sem novo flip (single-flip invariant).
