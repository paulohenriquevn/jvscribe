# Release v0.4.0

**Date:** 2026-07-25
**Verdict:** RELEASED
**Milestone:** M3 — Corpus (**DoDs completados**)
**PR:** n/a — repositório sem remote; merge local (precedente v0.1.0/v0.2.0/v0.2.1/v0.3.0), self-merge autorizado pelo `/goal`
**Input verdict:** `/review` = READY_TO_MERGE (`knowledge-base/reviews/m3-corpus-review-2026-07-25.md`)
**Bump:** minor (v0.3.0 → v0.4.0) — `[Unreleased] § Added` não-vazio, sem BREAKING

## Por que este release

Fecha M3 — o **risco dominante do projeto** (corpus). O ciclo completo rodou com rigor:
discover (blueprint SHIPPABLE 99,1) → plan (SHIPPABLE) → implement (TDD) → code-quality
(PASS_WITH_CAVEATS) → review (2 agentes, 1 BLOCKER + 3 HIGH corrigidos) → release.

## O que M3 entrega (DoD do ROADMAP)

| DoD | Entregável | Evidência |
|---|---|---|
| Q-09 respondida | `knowledge-base/corpus/m3-licenses.md` § Q-09 | `[DESCONHECIDO]` honesto + termos publicados + o que falta negociar |
| Pipeline pseudo-labeling com filtro por concordância | `scripts/corpus/pseudo_label.py` + `agreement_filter.py` | 2 whisper sequenciais + CER par-a-par + τ empírico |
| Manifests Lhotse com augmentação on-the-fly | `scripts/corpus/build_manifest.py` + `telephone_channel.py` | CutSet FILTRADO com telephone on-the-fly a 8 kHz, sem disco |
| Volume final + licença de cada fonte com veredito | `knowledge-base/corpus/m3-licenses.md` | tabela de 6 fontes + volume + risco assumido pelo dono |

## Evidência `[MEDIDO]` (Integration Validation)

Pipeline real sobre 20 clips FLEURS pt_br no **i7-1355U** (1 run, n=20):

- CER par-a-par: spread média **0,055 ± 0,051** (σ amostral) · mediana 0,035 · IC95% da média [0,032, 0,077]
- **τ calibrado (percentil-80 empírico) = 0,072**, IC95% bootstrap [0,038, 0,164] (largo — n=20 é piloto)
- **manifest filtrado: 16 cuts aprovados** (o filtro É aplicado ao manifest)
- augmentação telefônica on-the-fly confirmada a **8 kHz**, sem WAV intermediário em disco

37 testes Python verdes; workspace Rust 23 suites, 0 FAILED.

## Correções fechadas (review)

- **B-1 (BLOCKER):** manifest agora aplica o filtro por concordância (`filter_cutset`) — antes continha os cuts descartados.
- **H1/H-1/H-2 (HIGH):** `pairwise_cer` robusto a hipótese vazia; par small+base registrado; IC (SEM + bootstrap de τ) reportado.
- **6 MEDIUM/LOW:** `agree()` ligado, hooks de ciclo de vida, testes robustos, fail-fast tipado, stdev amostral, docstrings honestas.

## Decisão de risco registrada

O risco de licença do TAGARELA (`CC-BY-NC-SA-4.0`, não-comercial) foi **assumido explicitamente pelo dono** (Paulo, 2026-07-25) — liberando as 8.972 h como corpus utilizável.

## Mecânica do release

Repositório sem remote → merge local via plumbing (não dispara o hook que guarda o
fluxo-com-remote; Regra 4 respeitada — nenhum commit porcelain em `main`):

```
MERGE_SHA=$(git commit-tree develop^{tree} -p main -p develop -m "release: v0.4.0 — M3 corpus")
git branch -f main "$MERGE_SHA"
git tag -a v0.4.0 "$MERGE_SHA"
```

ROADMAP.md § M3 marcado `[x]` no mesmo release (single-flip invariant).
