# Release v0.2.0

**Date:** 2026-07-24
**Verdict:** RELEASED
**Milestone:** M1 — Instrumentação e régua de medição
**Source review:** `knowledge-base/reviews/m1-measurement-harness-review-2026-07-24.md` (READY_TO_MERGE)
**PR:** n/a — repositório sem remote; merge local (precedente de v0.1.0), self-merge autorizado pelo `/goal`
**Merge commit:** `a43e66a` (parents: main@v0.1.0 + develop; tree = develop)
**Tag:** v0.2.0 (anotada, aponta para o merge commit)
**GitHub release:** n/a (sem remote)

## Gates do ciclo

| Fase | Verdict |
|---|---|
| DISCOVER (blueprint) | SHIPPABLE (99.7) |
| PLAN (plan-confidence) | SHIPPABLE_WITH_CAVEATS (89.6) |
| deps-audit | PASS (zero CVE) |
| CODE-QUALITY | PASS_WITH_CAVEATS (D2-Rust N/A por compilador; cargo-udeps allowlisted) |
| REVIEW (5 agentes) | READY_TO_MERGE (0 BLOCKER; 1 HIGH + 6 MEDIUM de defeito corrigidos) |

## Entregue em M1

- **Harness de medição** (`crates/macaw-audio/src/harness.rs`): `RtfxMeter` (RTFx com warmup), `LatencyHistogram` (p50/p95/p99), `ThermalRatio` — erros tipados, sem panic. Modo `macaw-cli bench` + `scripts/bench.sh` (taskset P-cores). Medido: RTFx 17,81× quente vs 1,5× frio (janela curta; soak ≥10 min RNF-04 e carga RNF-05 seguem `[DESCONHECIDO]`).
- **Cadeia de augmentação telefônica 8 kHz** (`scripts/telephone_augment.sh`): 16k→8k + banda 300-3400 + G.711 a-law round-trip, teste determinístico.
- **WER com IC 95%** (`scripts/eval_wer.py` + `text_normalize_ptbr.py`): bootstrap por-utterance, reusa `jiwer`, normalizador PT-BR próprio.
- **Baseline real** (`scripts/baseline_minds14.py`): **WER = 73,0% [IC95: 49,8%–104,6%]** (faster-whisper-base sobre minds14 pt-PT, fala telefônica real 8 kHz), com proveniência `[MEDIDO]` e caveats honestos.

## Follow-ups de M1 (rastreados, não bloqueantes)

- Baseline dos 3 modelos (TAGARELA 2,3 GB já cacheado + Moonshine + whisper-large-v3).
- Soak real de ≥ 10 min sob carga concorrente (RNF-04/05).
- Test set pt-BR consentido (LGPD, fora do escopo atual).
- Numeral-por-extenso no normalizador PT-BR.

## Nota sobre o ambiente Python

Para o baseline real: `jiwer`/`faster-whisper`/`datasets` instalados; um diretório `torch` órfão (sem `__init__`, pip-untracked, remanescente de uninstall) foi movido para `torch.orphaned-bak` (reversível) para destravar o `datasets`. O baseline lê o parquet do HF direto (evita a dependência de `torchcodec` do `datasets` 5.0).
